const DATE_FORMAT = {
    DISPLAY_DATE: 'DD-MM-YYYY',
    API_DATE: 'YYYY-MM-DD',
};

const HOUR_FORMAT = {
    DISPLAY_HOUR: 'HH:mm:ss',
};

export default {
    DATE_FORMAT,
    HOUR_FORMAT,
    // FIREBASE,
};
